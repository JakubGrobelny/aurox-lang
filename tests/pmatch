defop + 10 left
defop / 15 left

type Maybe a with
    case Nothing
    case Just a
end

define (/) a b : Int -> Int -> Int =
    a
end

define safediv a b : Int -> Int -> Maybe Int =
    match 1 with
        case 0 => Nothing
        case _ => Just (a / b)
    end
end






