defop - 20 prefix
defop < 1 none

define (-) a : Int -> Int =
    a
end

define make_non_negative a : a -> NonNegativeInt =
    a
end

define f a : Int -> NonNegativeInt =
    make_non_negative (-a)
end

